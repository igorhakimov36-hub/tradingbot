"""
Research-only modules - never wired into production strategy, coordinator,
or backtest decision paths. Each module here is an explicitly-labeled
comparison variant or experimental policy, used only by research scripts
and their own test suites. See each module's own docstring for the
specific research question it exists to answer and its current status.
"""
